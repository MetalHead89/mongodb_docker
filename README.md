# Mongodb

## Run

Create container:

$ docker-compose up -d

Start container in interactive mode:

$ docker exec -it mongodb bash

Launching the shell:

$ mongosh

Stop container:

$ docker-compose down
